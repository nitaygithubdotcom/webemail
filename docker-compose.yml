version: "3.8"
services: 
    scrapy:
        container_name: scrapy
        image: scrapy:latest
        volumes: 
            - ./:/app/
        working_dir: /app/webemail
        # command: ["sleep","1000"]
        command: ["sleep","crawl","collectemail","--nolog"]